# unplugin-auto-import 生成的类型文件，每次提交都会改变，所以加入 .gitignore 配合使用
auto-import.d.ts

# vite-plugin-uni-pages 生成的类型文件，每次切换分支都会不一样，所以直接写入 .gitignore
uni-pages.d.ts

# 插件生成的文件
src/pages.json
src/manifest.json
